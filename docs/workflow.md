# Workflow

Follow these steps for every task:

1. Invoke the `update-flutter-dependencies` skill when the task starts, except
   for documentation-only work.
2. Complete the requested task.
3. Run the relevant available tests for the change.
4. Ask for explicit confirmation before creating a commit, then commit the
   requested changes.
